**API (Контракт):** * Это самое важное место, где мы пересекаемся. 
Создаём карточку-заметку, где будем фиксировать: «Я отправляю запрос по адресу `/get_data`, ты мне возвращаешь вот такой JSON (в разработке)