
- **База и подложки:** глубокий полночный фон (`#020617`), контентные контейнеры (`#0f172a`) и карточки (`#1e293b`).
    
- **Акценты:** небесно-голубой (`#38bdf8`) для фокусов, ссылок и активных состояний, изумрудный (`#10b981`) для денежных или успешных маркеров.
    
- **Геометрия:** мягкие скругления блоков (`border-radius: 12px` / `16px`), аккуратные внутренние отступы (`14px`–`16px`) и железная мобильная адаптивность без горизонтальных скроллов.