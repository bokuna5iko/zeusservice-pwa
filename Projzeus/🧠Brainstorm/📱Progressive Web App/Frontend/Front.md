# 🎨 Frontend (Визуальная часть и интерфейс)

Этот файл описывает внешний вид PWA, стилистику и структуру экранов.

## 🌈 Дизайн-система (UI Kit)
Чтобы приложение выглядело цельно, придерживаемся следующих параметров:

- **Цветовая палитра:**
  - `Primary (Main):` [Например, Темно-синий/Черный] — для фона.
  - `Accent:` [Например, Ярко-зеленый или Золотой] — для активных карточек и кнопки "Бесплатная мойка".
  - `Text:` Белый/Светло-серый (для контраста с темным фоном).
- **Шрифты:** Без засечек (Sans-serif), читабельные на ходу.

## 📱 Структура экранов (Layout)

### 1. Главный экран (Dashboard)
- **Счетчик посещений:** Визуальные иконки (например, 8 пустых кружков/машинок, которые заполняются цветом по мере визитов).
- **Инфо-блок:** "Осталось [N] моек до подарка".

### 2. Экран поздравления (Loyalty Popup)
- Появляется, когда `counter == 8`.
- **Элементы:** Большая надпись "ПОЗДРАВЛЯЕМ!", яркая подсветка, кнопка активации.

## 🏗 Верстка и CSS
- **Адаптивность:** Mobile First! (Приложение должно идеально выглядеть на iPhone и Android).
- **Flexbox/Grid:** Используем для центрирования карточек и макета.
- **Анимации:** - Плавный переход (transition) при заполнении счетчика.

## 🖼 Ассеты (Графика)
- Все изображения хранятся в `/assets/img/`.
- Иконки PWA: `/assets/icons/`.

---

## ⚡️ Текущие задачи по фронтенду:

1. [ ] Настроить CSS-переменные для легкой смены темы.
2. [ ] Проверить отображение карточки поздравления на маленьких экранах (SE/Mini).

# Главный экран

- **QR-идентификатор:** Генерируется на основе ID пользователя и текущей даты (обновление каждые 24 часа), имеет функцию увеличения при клике.
    
- **Интерактивная сетка визитов:** 8 сегментов, которые подтягиваются из базы данных (`visit_count`), с визуальными акцентами на 4-м визите (скидка) и 8-м визите (подарок).
    
- **Синхронизация данных:** Бэкенд передает актуальные значения бонусов и визитов прямо в объект пользователя при авторизации.
    
- **Чистота кода:** Устранили путаницу между `db` и `pool`, привели всё к единому стандарту.
# Страница История

### Итоги модуля «История»:

1. **База данных:** Добавлена колонка `visit_number` в таблицу `visits` для точной фиксации этапа лояльности в момент оказания услуги.
    
2. **Бэкенд:** Контроллер `getUserHistory` теперь отдает расширенный набор данных (`service_name`, `base_price`, `visit_number`, `created_at`).
    
3. **Frontend (UI/UX):** * Реализован компонент-аккордеон `VisitItem` с плавной анимацией раскрытия.
    
    - **Умная логика цен:** Система автоматически определяет 4-й и 8-й визиты в истории, перечеркивает базовую цену и подставляет скидочную (20% или 100%).
        
    - **Сводная статистика:** Верхний контейнер на странице истории теперь отображает реальное общее количество заездов пользователя.
        

---

**Текущий статус проекта:**

- [x] Авторизация и профиль
    
- [x] Главный экран (QR + Сетка лояльности)
    
- [x] История визитов (Список с деталями)
    
- [ ] Бонусный баланс (В процессе)
    
- [ ] Личные данные / Автомобили (В планах)

## ТЗ на разработку модуля «Интерактивный прейскурант»

### 1. Общие требования к интерфейсу

- **Компонент:** Модальное окно (Modal Overlay).
    
- **Цветовая схема:** Темный фон (Deep Navy/Black), белый и золотистый/серебристый акценты (согласно логотипу Zeus Auto).
    
- **Шрифты:** Без засечек (например, Inter или Roboto) для высокой читаемости на мобильных устройствах.
    

### 2. Функциональные блоки модального окна

#### 2.1 Шапка (Header)

- Заголовок: «Прейскурант услуг».
    
- Кнопка закрытия (крестик) в верхнем правом углу.
    
- **Селектор классов (Sticky Bar):** Горизонтальный переключатель (Tabs), который фиксируется наверху при скролле.
    
    - Элементы: `1 класс`, `2 класс`, `3 класс`, `4 класс`, `5 класс`.
        
    - _Поведение:_ По умолчанию выбран «1 класс». При смене класса цены в списке ниже обновляются мгновенно (без перезагрузки окна).
        

#### 2.2 Список услуг (Content)

Услуги делятся на две логические группы:

**Группа №1: Услуги с градацией по классам**

- _Список:_ 2-х фазная мойка, Стандартная мойка + коврики, Комплексная мойка, Мойка днища.
    
- _Отображение:_ Название услуги (крупно) + Цена (ярко).
    
- _Логика цены:_ Берется из массива данных в зависимости от активного таба в Header.
    

**Группа №2: Услуги с фиксированной ценой**

- _Список:_ Все остальные (Мойка мотоцикла, двигателя, химчистка, пылесос и т.д.).
    
- _Отображение:_ Название + Цена (например, "от 500 ₽"). Цена не меняется при переключении классов.
    

#### 2.3 Система описаний (Details)

- Использовать механизм **Accordion (Аккордеон)**.
    
- Рядом с названием услуги (или под ним) находится иконка стрелочки или «i».
    
- При клике на строку услуги раскрывается блок с подробностями (тест из прайса).
    
    - _Пример для 2-х фазной мойки:_ «Мойка эмульсией, мойка пенным кислотным шампунем, полная сушка, чернение резины».
        

---

### 3. Структура данных (JSON)

Для удобства поддержки рекомендуется хранить данные в таком формате:

JavaScript

```
const priceList = {
  classBased: [
    {
      id: "wash_2phase",
      title: "2-х фазная мойка кузова",
      description: "Мойка эмульсией; Мойка пенным шампунем; Полная сушка; Чернение резины.",
      prices: { 1: 2000, 2: 2500, 3: 3000, 4: 3500, 5: 4000 }
    },
    // ... остальные 3 услуги
  ],
  fixed: [
    {
      id: "moto_wash",
      title: "Мойка мотоцикла",
      description: "Бережная мойка двухколесного транспорта",
      price: "от 500 ₽"
    },
    // ... все остальные услуги
  ]
};
```

---

### 4. Технические рекомендации (UX/UI)

1. **Haptic Feedback:** Если это PWA, при переключении классов можно добавить легкую вибрацию (через Vibration API), чтобы имитировать нажатие физической кнопки.
    
2. **Визуальный акцент:** При смене класса цена должна подсвечиваться (например, кратковременное изменение цвета или легкая анимация появления), чтобы пользователь заметил изменения.
    
3. **Адаптивность:** На мобильных устройствах сделать модальное окно «шторкой» (BottomSheet), которая выезжает снизу и закрывает 90% экрана.
    

### 5. План реализации (MVP)

- [x] Подготовка JSON-файла со всеми данными из фото.
    
- [x] Верстка базового модального окна в React-компоненте.
    
- [x] Реализация логики переключения `activeClass` (useState).
    
- [x] Настройка условий: если у услуги есть поле `prices`, выводим цену по ключу, если нет — выводим статичное поле `price`.


# 6. Профиль: Этап 1 и Модальные окна"

- **Личные данные:** Смена аватара (из 5 вариантов), редактирование имени с сохранением в БД и отображение номера телефона.
    
- **Статистика:** Вывод статуса, общего количества визитов и корректное форматирование даты регистрации.
    
- **Интерфейс действий:** Создан контейнер с кнопками «Прейскурант», «Настройки» и «Выход».
    
- **Система модалок:** Реализовано универсальное модальное окно с эффектом размытия фона (`backdrop-filter`) и анимацией появления.
    
- **Бэкенд:** Исправлены контроллеры и роуты для корректной передачи всех данных пользователя и обработки обновлений.


# 7. Профиль: Этап 1 и Прейскурант"

### ТЗ:
1. Общие требования к интерфейсу

Компонент: Модальное окно (Modal Overlay).

Цветовая схема: Темный фон (Deep Navy/Black), белый и золотистый/серебристый акценты (согласно логотипу Zeus Auto).

Шрифты: Без засечек (например, Inter или Roboto) для высокой читаемости на мобильных устройствах.

2. Функциональные блоки модального окна

2.1 Шапка (Header)

Заголовок: «Прейскурант услуг».

Кнопка закрытия (крестик) в верхнем правом углу.

Селектор классов (Sticky Bar): Горизонтальный переключатель (Tabs), который фиксируется наверху при скролле.Элементы: 1 класс, 2 класс, 3 класс, 4 класс, 5 класс.

Поведение: По умолчанию выбран «1 класс». При смене класса цены в списке ниже обновляются мгновенно (без перезагрузки окна).

2.2 Список услуг (Content)

Услуги делятся на две логические группы:

Группа №1: Услуги с градацией по классам

  

Список: 2-х фазная мойка, Стандартная мойка + коврики, Комплексная мойка, Мойка днища.

Отображение: Название услуги (крупно) + Цена (ярко).

Логика цены: Берется из массива данных в зависимости от активного таба в Header.

Группа №2: Услуги с фиксированной ценой

  

Список: Все остальные (Мойка мотоцикла, двигателя, химчистка, пылесос и т.д.).

Отображение: Название + Цена (например, "от 500 ₽"). Цена не меняется при переключении классов.

2.3 Система описаний (Details)

Использовать механизм Accordion (Аккордеон).

Рядом с названием услуги (или под ним) находится иконка стрелочки или «i».

При клике на строку услуги раскрывается блок с подробностями (тест из прайса).Пример для 2-х фазной мойки: «Мойка эмульсией, мойка пенным кислотным шампунем, полная сушка, чернение резины».

3. Структура данных (JSON)

Для удобства поддержки рекомендуется хранить данные в таком формате:

JavaScript

```json
  

const priceData = {
  classBased: [
    {
      id: "wash_2phase",
      title: "2-х фазная мойка",
      description: "Мойка эмульсией; Мойка пенным шампунем; Полная сушка кузова; Чернение резины.",
      prices: { 1: 800, 2: 900, 3: 1000, 4: 1100, 5: 1200 }
    },
    {
      id: "standard_plus",
      title: "Стандарт + коврики",
      description: "Бесконтактная мойка кузова; Промывка резиновых ковриков; Сушка порогов и зеркал.",
      prices: { 1: 600, 2: 700, 3: 800, 4: 900, 5: 1000 }
    },
    {
      id: "complex",
      title: "Комплексная мойка",
      description: "2-х фазная мойка; Уборка салона пылесосом; Влажная уборка пластика; Мойка стекол изнутри.",
      prices: { 1: 1500, 2: 1800, 3: 2100, 4: 2400, 5: 2700 }
    },
    {
      id: "chassis",
      title: "Мойка днища",
      description: "Промывка нижней части автомобиля на подъемнике или специализированной насадкой.",
      prices: { 1: 500, 2: 500, 3: 600, 4: 700, 5: 800 }
    }
  ],
  fixed: [
    { id: "moto", title: "Мойка мотоцикла", description: "Деликатная мойка узлов и агрегатов.", price: "от 500 ₽" },
    { id: "engine", title: "Мойка двигателя", description: "Чистка составами-диэлектриками и консервация.", price: "2500 ₽" },
    { id: "dry_clean", title: "Химчистка (1 элемент)", description: "Глубокая очистка ткани или кожи спецсоставами.", price: "от 1000 ₽" },
    { id: "vacuum", title: "Пылесос салона", description: "Тщательная уборка пола, сидений и багажника.", price: "400 ₽" }
  ]
};
```
4. Технические рекомендации (UX/UI)

Haptic Feedback: Если это PWA, при переключении классов можно добавить легкую вибрацию (через Vibration API), чтобы имитировать нажатие физической кнопки.

Визуальный акцент: При смене класса цена должна подсвечиваться (например, кратковременное изменение цвета или легкая анимация появления), чтобы пользователь заметил изменения.

Адаптивность: На мобильных устройствах сделать модальное окно «шторкой» (BottomSheet), которая выезжает снизу и закрывает 90% экрана.

5. План реализации (MVP)

Подготовка JSON-файла со всеми данными из фото.

Верстка базового модального окна в React-компоненте.

Реализация логики переключения activeClass (useState).

Настройка условий: если у услуги есть поле prices, выводим цену по ключу, если нет — выводим статичное поле price.


# Админ аккаунт

Продолжаем работу

Мы с тобой в последний раз закончили работать над тем как клиент видит приложение.

Теперь переходим к настройке аккаунта админа

Заходя через админский аккаунт, у нас будут те же вкладки что и у клиента, но с другим немного наполнением

### Краткое ТЗ

1. Блок "Главная" делится на 4 контейнера:

Контейнер №1

Находится в самом верху, в нем будет прогресс бар линейный который показывает "Цель" или же простым языком "План на смену"

Логика: Босс Артём обозначил, что 60 машин в день это золотая стабильная середина

UI: линейный прогресс бар с счётчиком машин. На шкале есть метки, или же контрольные точки которые показывают что на определённой точке n количество машин и т.д. Главные ключевые точки будут разделены на 30, 60, 75 и 90 (75 и 90 выделены визуально, что бы подчеркнуть что это перевыполнение плана)

На шкале есть курсор который в реальном времени считает визиты совершённые сегодня и показывает настоящее число. Он двигается в соответствии набора машин сделанных сегодня.

  

Контейнер №2.

Расположение: находится чуть ниже середины экрана для удобства большого пальца.

Логика: Служит кнопкой которая позволяет включить камеру и отсканировать QR клиента. Далее после сканирования открывается модальное окно "Калькулятор" который позволяет быстро начислить визит, исходя от типа услуги и счётчика визита который проверяет "Карту лояльности". Если посещение не является скидочным (то есть 4, или же 8) то при выборе услуги пишется базовая цена и числовое значения visit_count (которое отвечает за слежку карты лояльности). Если визит является 4, то сумма при выборе услуги изменяется и дублируется на 2 значения (Дефолт цена и Дефолт цена + скидка. В нашем случае 4 посещение является скидкой в 20%, соответственно дефолт цена + скидка будет показывать цену на 20% меньше). Если после выбора услуги у клиента 8 визит (бесплатный) то как и в случае выше, цена дублируется на 2 и в дефолтном + скидка цена при оплате будет 0 рублей.

Если цена не соответствуют выбору представленном в калькуляторе, то есть способ вручную вбить цену

Так же в модальном окне после выбора услуги и перед зачислением, будет выбор оплаты (пока что просто для учёта) онлайн перевод или наличными.

После нажатия "Зачислить визит" данные уходят в базу

UI: Выглядит как квадрат, визуально подчёркивающий что это кнопка для сканироваия QR.

После успешного начисления визита, появляется визуальное отображение что данные были отправлены и визит зачислен

  

Контейнер №3

Расположение: Находится прямо под контейнером №2

Логика: Кнопка для быстрого добавление в базу без использования QR с последующим открыванием калькулятора. В нем калькулятор видит что клиента нет в базе и создаёт временный отдельный ID отличающийся от клиентов что используют приложения (показывают QR). Клиенты с временным ID ведутся для учёта и сбора данных (купленные услуги, дата визита)

Временный отдельный ID создаётся только единожды и его задача только отправить данные.

Временный ID не привязывается к номеру

UI: выглядит как прямоугольная кнопка прям под qr

  

Контейнер №4

Расположение: Находится внизу экрана

Логика: Служит краткой историей из 3 последних сканирований администратора. В нём будут собраны те же блоки что и будут в дальнейшем созданы на странице "История"

Он служит для понимания и напоминания администратору о сделанном или не сделанном сканировании.

UI: Берём как референс контейнеров и подконтейнеров в "Истории" у аккаунта клиента